mutation AddClient($input: AddClientInput!) {
  addClient(input: $input) {
    id
    name
    email
    phone
    birthdate
    age
    waiver
    membershipStatus
    product {
      name
      description
      price
    }
  }
}